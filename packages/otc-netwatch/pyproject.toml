[project]
name = "otc-netwatch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests==2.32.3",
]

[project.scripts]
otc-netwatch = "otc_netwatch.cli:main"

[build-system]
requires = ["hatchling>=1.30.1,<2"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "types-requests==2.32.0.*",
]
